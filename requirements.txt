# Phien ban khoa cho deploy on dinh (tranh "code khong doi ma deploy hong").
# Cap nhat co chu dich: doi so roi chay test + deploy thu.
discord.py==2.7.1
python-dotenv==1.2.2
asyncpg==0.31.0
flask==3.1.3
aiohttp==3.14.1
requests==2.34.2
# --- pipeline PDF cu (chi watcher dung; se go o C3) ---
pypdf==6.14.2
pymupdf==1.28.0
pytesseract==0.3.13
Pillow==12.3.0
img2pdf==0.6.3
pikepdf==10.9.1
